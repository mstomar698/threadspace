# Base URL of the Django API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket base URL of the Rust realtime gateway (no trailing slash).
# Leave unset to disable the live feed; the app still works without it.
NEXT_PUBLIC_REALTIME_URL=ws://localhost:8080
